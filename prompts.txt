The aim 01: Define all TypeScript interfaces for DummyJSON API responses without using `any`
Actual prompt: "I'm building a Vue 3 + TypeScript SPA consuming the DummyJSON API (/products, /products/:id, /auth/login). Generate strict TypeScript interfaces for Product (including nested Review, ProductDimensions, ProductMeta), ProductsResponse, Category (slug/name/url), CartItem, AuthUser (with accessToken/refreshToken), and LoginCredentials. Do not use `any` anywhere."

The aim 02: Set up Pinia stores for auth and cart with localStorage persistence
Actual prompt: "Create two Pinia stores using the Composition API (defineStore with setup function style). 1) authStore: manages AuthUser | null state, has init() to restore from localStorage, async login() that POSTs to https://dummyjson.com/auth/login and saves the JWT, logout() that clears state and localStorage. 2) cartStore: manages CartItem[], persists to localStorage using a deep watch, exposes addToCart, removeFromCart, incrementQuantity, decrementQuantity, clearCart, isInCart, getQuantity, totalItems (computed), totalPrice (computed). All types strictly typed, no `any`."

The aim 03: Create a module-level singleton composable for shared filter state
Actual prompt: "In Vue 3 with TypeScript, create a composable useFilters.ts that shares searchQuery (Ref<string>) and selectedCategory (Ref<string>) across all components by declaring them at module level (outside the function). Include setCategory(slug) that also clears search, setSearch(query), and clearSearch(). Explain why this singleton pattern avoids prop-drilling without needing Pinia."

The aim 04: Build the NavBar component with search, dark mode toggle, auth status, and cart badge
Actual prompt: "Write a Vue 3 TypeScript NavBar.vue using Composition API (<script setup lang='ts'>). It should: use useAuthStore and useCartStore from Pinia; use a useDarkMode composable to toggle between sun/moon SVG icons; include a desktop search bar and a collapsible mobile search bar; emit 'open-login' event; show a cart icon with an animated badge for item count; show the user's avatar and a sign-out button when authenticated. Use only Tailwind CSS classes including dark: variants. No inline styles."

The aim 05: Create a ProductCard component with discount badge, star rating, add-to-cart feedback
Actual prompt: "Write a Vue 3 TypeScript ProductCard.vue. Props: { product: Product }. Features: lazy-loaded thumbnail with SVG fallback on @error; a red discount badge (visible when discountPercentage >= 5); an in-cart checkmark badge; a StarRating component; show discounted price and crossed-out original; an 'Add to Cart' button that shows a green '✓ Added!' state for 1.5s using a ref and setTimeout; if already in cart show 'Add More' style. Use Tailwind CSS, hover:-translate-y-1 card lift, group class for image zoom on hover."

The aim 06: Design the CartDrawer slide-out panel
Actual prompt: "Create CartDrawer.vue in Vue 3 TypeScript. It should: use <Teleport to='body'>; show a backdrop overlay and a slide-in panel from the right using Vue <Transition> with translateX CSS classes; read state from cartStore (items, totalPrice, totalItems, isEmpty); for each CartItem show thumbnail, title, per-item price, and quantity +/- controls using incrementQuantity/decrementQuantity; include a remove button; show a savings banner if discounts apply; show subtotal, free delivery line, and a total; include a 'Proceed to Checkout' button (visual only). Fully dark-mode compatible."

The aim 07: Implement the ProductView detail page with image gallery, reviews, and quantity selector
Actual prompt: "Write ProductView.vue for Vue 3 TypeScript. It receives id as a string prop (from Vue Router). Use a useProduct composable that calls GET https://dummyjson.com/products/:id. Show: a thumbnail gallery (main image + clickable thumbnails); category badge and tags; title, brand, star rating, review count; a price block with discounted price, strikethrough original, discount % badge, savings amount; stock status pill; a quantity selector (disabled when quantity >= stock); an 'Add to Cart' button that adds quantity items and shows success state; a 'View Cart' shortcut when product is already in cart; shipping/warranty/return/weight info cards; and a reviews grid with per-review star rating, reviewer name, date, and comment. Support dark mode."

The aim 08: Add Tailwind dark mode configuration
Actual prompt: "Configure Tailwind CSS v3 for class-based dark mode in a Vite + Vue 3 project. Show the tailwind.config.js with darkMode: 'class', content paths covering .vue files, and a custom primary color palette (green-based). Also show a useDarkMode.ts composable that reads/writes localStorage, adds/removes the 'dark' class on document.documentElement, and detects prefers-color-scheme as the default if no value is stored. Use module-level singleton pattern so state is shared."

The aim 09: Debug TypeScript errors in Vue 3 defineEmits with the new typed syntax
Actual prompt: "In Vue 3.4+ with TypeScript, what is the correct way to type defineEmits for an event with no payload (like 'close') and an event with a payload (like 'open-login')? Show the correct syntax using the generic type argument form, e.g. defineEmits<{ close: []; 'open-login': [] }>() and explain why the array syntax is used."

The aim 10: Optimise the FilterBar to load categories from the API and highlight active selection
Actual prompt: "Write FilterBar.vue in Vue 3 TypeScript. It should call fetchCategories() (which hits GET https://dummyjson.com/products/categories returning Category[]) on onMounted. Render a horizontally scrollable row of pill buttons. The first pill is 'All Products' with slug 'all'. Use useFilters() to read selectedCategory and call setCategory(slug) on click. Active pill: bg-primary-600 text-white. Inactive pill: bg-gray-100 hover:bg-gray-200. Include dark mode variants. Use overflow-x-auto with a thin custom scrollbar utility class."
